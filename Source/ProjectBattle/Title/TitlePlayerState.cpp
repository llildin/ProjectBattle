// Fill out your copyright notice in the Description page of Project Settings.


#include "Title/TitlePlayerState.h"
#include "Lobby/LobbyPlayerState.h"

/*
void ATitlePlayerState::CopyProperties(APlayerState* NewPlayerState)
{
    UE_LOG(LogTemp, Warning, TEXT("DDD"));
    Super::CopyProperties(NewPlayerState);

    if (NewPlayerState)
    {
        ALobbyPlayerState* NewLobbyPS = Cast<ALobbyPlayerState>(NewPlayerState);
        if (NewLobbyPS)
        {
            NewLobbyPS->Nickname = this->Nickname;
        }
    }
}
*/
